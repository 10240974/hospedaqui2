# usando o git-exemplo

Um projeto de gestão de hospedagem

### install

$ pip install -r requirements.txt

### run

$ flask --app main run

##python -m streamlit run hospedaqui.py --server.runOnSave tru